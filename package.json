{
  "name": "heavy-sync",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.functionapp.js",
  "scripts": {
    "start": "tsx src/index.ts --run",
    "test": "vitest --run",
    "build": "esbuild src/index.ts --bundle --platform=node --outdir=dist",
    "clean": "rimraf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@spotify/web-api-ts-sdk": "^1.1.2",
    "command-line-args": "^5.2.1",
    "cruet": "^0.2.1",
    "dotenv": "^16.3.1",
    "tsx": "^4.6.1",
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.11",
    "rimraf": "^5.0.0",
    "vitest": "^0.31.4"
  }
}
