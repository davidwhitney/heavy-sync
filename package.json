{
  "name": "heavy-sync",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsx src/index.ts --run",
    "test": "vitest --run",
    "build": "esbuild src/index.ts --bundle --platform=node --outdir=dist && xcopy deploy\\*.* dist /F /-I /Y && xcopy .env dist\\.env /F /-I /Y",
    "deploy": "scp -rp dist Administrator@redqueen:c:\\apps\\heavy-sync",
    "publish": "npm run build && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@spotify/web-api-ts-sdk": "^1.1.2",
    "command-line-args": "^5.2.1",
    "cruet": "^0.2.1",
    "dotenv": "^16.3.1",
    "tsx": "^4.6.1",
    "typescript": "^5.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.19.11",
    "vitest": "^0.31.4"
  }
}
